# ── Sensitive / personal — NEVER commit ─────────────────────────────
# OAuth client secret & refresh token
Token.txt
oauth.json
*.token
*.secret

# Your real broker data (positions, buying power) — regenerated by the script
portfolio.json
dashboard.html

# ── Build / cache ───────────────────────────────────────────────────
__pycache__/
*.py[cod]
.DS_Store
